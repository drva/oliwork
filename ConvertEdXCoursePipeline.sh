
# ==CONTENT==

# Run the main java converter

# Convert LO's, if they exist

# Move the LO files, if they exist, so they's in the same folder as the XSLT converter

# Run the main xslt converter

# (Optional) process errors from the above

# Make the protooutline into an outline

# Run inline activity java if needed (not needed for activities any more, still needed for submit and compare)

# Do more stuff re submit and compare

# Library content if it exists should be processed at some point?

# Name changing if it's long enough (usually do after everything is valid when doing by hand, but)

# ==PROBLEMS==

# Run the multipart splitter

# Run the xslt converter